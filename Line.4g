// Linha.g4

grammar Linha;

// Regras de sintaxe
programa: linha+;
linha: NOME_LINHA ':' texto;

// Definição de tokens
texto: STRING;
NOME_LINHA: [A-Za-z]+;
STRING: '"' .*? '"';
WS: [ \t\r\n]+ -> skip;

